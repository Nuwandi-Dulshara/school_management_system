@extends('layouts.super_admin', ['pageTitle' => 'Exam Report'])
@section('super_admin_content')
@include('reports._styles')
<div class="page-heading"><div><h1>Exam Report</h1><p>Exam periods, types, publishing state, and schedule readiness.</p></div>@include('reports._toolbar')</div>
<div class="card report-card mb-4 report-filters"><div class="card-body"><form class="row g-3">@include('reports._common_filters', ['fields' => ['academic_year','class_id','section_id']])<div class="col-md-3"><label class="form-label">Exam Type</label><select name="exam_type_id" class="form-select"><option value="">All types</option>@foreach($examTypes as $type)<option value="{{ $type->id }}" @selected((string)request('exam_type_id')===(string)$type->id)>{{ $type->name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Exam</label><select name="exam_id" class="form-select"><option value="">All exams</option>@foreach($examsFilter as $exam)<option value="{{ $exam->id }}" @selected((string)request('exam_id')===(string)$exam->id)>{{ $exam->exam_name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">From</label><input type="date" name="date_from" value="{{ request('date_from') }}" class="form-control"></div><div class="col-md-3"><label class="form-label">To</label><input type="date" name="date_to" value="{{ request('date_to') }}" class="form-control"></div><div class="col-12 d-flex gap-2"><button class="btn btn-academic">Apply Filters</button><a href="{{ route('reports.exams') }}" class="btn btn-outline-secondary">Reset</a></div></form></div></div>
<div class="card report-card"><div class="card-body table-responsive"><table class="table table-hover report-table"><thead><tr><th>Exam</th><th>Type</th><th>Year</th><th>Class / Section</th><th>Period</th><th>Schedule</th><th>Status</th></tr></thead><tbody>@forelse($examRows as $exam)<tr><td class="fw-semibold">{{ $exam->exam_name }}</td><td>{{ $exam->examType?->name }}</td><td>{{ $exam->academic_year }}</td><td>{{ $exam->schoolClass?->name }} / {{ $exam->schoolSection?->name }}</td><td>{{ $exam->start_date?->format('M d') }} - {{ $exam->end_date?->format('M d, Y') }}</td><td><span class="badge text-bg-{{ $exam->schedules_count ? 'success' : 'warning' }}">{{ $exam->schedules_count ? 'Scheduled' : 'Not Scheduled' }}</span></td><td>{{ ucfirst($exam->status) }}</td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No exams found.</td></tr>@endforelse</tbody></table></div></div>
@if($examRows->hasPages())<div class="mt-4">{{ $examRows->links() }}</div>@endif
@endsection
