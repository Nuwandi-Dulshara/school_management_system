@extends('layouts.super_admin', ['pageTitle' => 'Teacher Report'])
@section('super_admin_content')
@include('reports._styles')
<div class="page-heading"><div><h1>Teacher Report</h1><p>Teacher assignments, subjects, contact information, and status.</p></div>@include('reports._toolbar')</div>
<div class="card report-card mb-4 report-filters"><div class="card-body"><form class="row g-3"><div class="col-md-3"><label class="form-label">Search</label><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Name or employee no."></div>@include('reports._common_filters', ['fields' => ['class_id','subject_id']])<div class="col-md-3"><label class="form-label">Status</label><select name="status" class="form-select"><option value="">All statuses</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select></div><div class="col-12 d-flex gap-2"><button class="btn btn-academic">Apply Filters</button><a href="{{ route('reports.teachers') }}" class="btn btn-outline-secondary">Reset</a></div></form></div></div>
<div class="card report-card"><div class="card-body table-responsive"><table class="table table-hover report-table"><thead><tr><th>Employee No.</th><th>Teacher</th><th>Classes</th><th>Subjects</th><th>Contact</th><th>Email</th><th>Status</th></tr></thead><tbody>@forelse($teachers as $teacher)<tr><td>{{ $teacher->employee_number }}</td><td class="fw-semibold">{{ $teacher->full_name }}</td><td>{{ $teacher->teachingAssignments->pluck('schoolClass.name')->filter()->unique()->join(', ') ?: '-' }}</td><td>{{ $teacher->teachingAssignments->pluck('subject.name')->filter()->unique()->join(', ') ?: '-' }}</td><td>{{ $teacher->contact_number ?? '-' }}</td><td>{{ $teacher->email ?? '-' }}</td><td><span class="badge text-bg-{{ $teacher->status === 'active' ? 'success' : 'secondary' }}">{{ ucfirst($teacher->status) }}</span></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No teacher records found.</td></tr>@endforelse</tbody></table></div></div>
@if($teachers->hasPages())<div class="mt-4">{{ $teachers->links() }}</div>@endif
@endsection
